===== Representation METS file metadata section (element fileSec)
The CSIP does not make use of the METS fileSec element mandatory, but it is strongly recommended. In CITS 3D HM the use of the METS fileSec element at the Representation  level becomes mandatory, such as to reference the file groups containing data, Paradata Documentation, Authentication Documentation and Other Documentation.
